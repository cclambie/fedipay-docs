# FediPay Docs

This repo hosts the documentation for FediPay.

- Live site: https://docs.fedipay.net/ (once DNS is configured)
- GitHub Pages preview: enabled via Actions

## Local dev

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
mkdocs serve
